// use core::dict::Felt252Dict;
mod controlflow;
mod loops;
mod practicingdatatypes;
mod collections;
mod dictionaries;

fn main() {
    // commnet the one you dont need to run.
    // controlflow::run();

    // practicingdatatypes::main();
    // loops::basic();
    // collections::main();
    dictionaries::main();
    // let mut grades: Felt252Dict<u64> = Default::default();

    // grades.insert('maria', 65);
// grades.insert('paul', 95);
// grades.insert('kelvin', 20);

    // let kelvin_score = grades.get('kelvin');

    // println!("kelvin scoers: {} ", kelvin_score);
}
